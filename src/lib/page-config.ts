import { headers } from "next/headers";
import type { PublishedPageConfig } from "@/types/site";
const fallback: PublishedPageConfig = { pageSlug:"home", enabled:true, preset:"DEFAULT_HOME", published:{seo:{},sections:["hero","search","property-categories","promotional-banner","destinations","featured-properties","why-choose-us","cta"].map((type,order)=>({id:`${type}-${order+1}`,type:type as PublishedPageConfig["published"]["sections"][number]["type"],enabled:true,order,config:{}}))} };
const hostname = (value:string|null) => (value||"").split(",")[0].trim().toLowerCase().replace(/^https?:\/\//,"").split("/")[0].split(":")[0];
export async function getPublishedPage(page="home"):Promise<PublishedPageConfig>{ const h=await headers();const host=hostname(h.get("x-forwarded-host")||h.get("host"));if(!host||["localhost","127.0.0.1"].includes(host))return fallback;const base=(process.env.BACKEND_INTERNAL_URL||process.env.NEXT_PUBLIC_API_URL||"http://127.0.0.1:5001").replace(/\/$/,"");const response=await fetch(`${base}/api/v1/sites/current/pages/${page}`,{headers:{"x-forwarded-host":host},next:{revalidate:300,tags:[`page:${host}:${page}`]}}).catch(()=>null);if(!response?.ok)return fallback;const payload=await response.json() as {data?:PublishedPageConfig};return payload.data?.published?.sections?payload.data:fallback;}
