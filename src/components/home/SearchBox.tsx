"use client";
import { CalendarDays, MapPin, Search, Users } from "lucide-react"; import { useRouter } from "next/navigation"; import { useState } from "react";
export function SearchBox(){const router=useRouter();const [destination,setDestination]=useState(""); const [checkIn,setCheckIn]=useState("2026-08-24");const [checkOut,setCheckOut]=useState("2026-08-27");const submit=(e:React.FormEvent)=>{e.preventDefault();const q=new URLSearchParams({destination:destination||"Goa",checkIn,checkOut,guests:"2"});router.push(`/hotels?${q}`)};return <form onSubmit={submit} className="search-panel"><label className="search-field"><span>Where are you going?</span><div><MapPin/><input value={destination} onChange={e=>setDestination(e.target.value)} placeholder="City, hotel or landmark"/></div></label><label className="search-field"><span>Check-in</span><div><CalendarDays/><input type="date" value={checkIn} onChange={e=>setCheckIn(e.target.value)}/></div></label><label className="search-field"><span>Check-out</span><div><CalendarDays/><input type="date" value={checkOut} onChange={e=>setCheckOut(e.target.value)}/></div></label><label className="search-field"><span>Guests & Rooms</span><div><Users/><select defaultValue="2"><option value="2">2 Guests, 1 Room</option><option value="1">1 Guest, 1 Room</option><option value="4">4 Guests, 2 Rooms</option></select></div></label><button className="btn-primary h-14 justify-center md:h-16"><Search className="size-4"/>Search Hotels</button></form>}
