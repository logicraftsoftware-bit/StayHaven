"use client";
import { useSite } from "@/components/site/SiteProvider";
import { SearchBox } from "./SearchBox";
export function Hero(){const site=useSite();return <section className="hero"><div className="container relative z-10 flex min-h-[520px] flex-col justify-center pb-26 pt-14 text-white"><p className="mb-3 text-xs font-bold uppercase tracking-[.25em] text-red-100">{site.tagline || `${site.city}'s stays, one search away`}</p><h1 className="font-display max-w-2xl text-4xl font-bold leading-[1.08] md:text-6xl">{site.heroTitle || <>Find Your <span className="text-gold">Perfect Stay</span><br/>In {site.city}</>}</h1><p className="mt-5 max-w-lg text-base text-slate-200 md:text-lg">{site.heroSubtitle || `Discover hotels, homestays, resorts, villas and unique stays across ${site.city}.`}</p><div className="hero-search absolute inset-x-4 -bottom-14 md:inset-x-0"><SearchBox/></div></div></section>}
