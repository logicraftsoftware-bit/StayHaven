import Link from "next/link";
import { Search, SlidersHorizontal } from "lucide-react";
import { getPublicProperties, getPublicPropertyTypes } from "@/lib/public-marketplace";
import { getCurrentSite } from "@/lib/site";
import { PublicPropertyCard } from "./PublicPropertyCard";

export async function Results({ destination, type, keyword, minPrice, maxPrice, page = "1" }: { destination?: string; type?: string; keyword?: string; minPrice?: string; maxPrice?: string; page?: string }) {
  const [site, result, types] = await Promise.all([getCurrentSite(), getPublicProperties({ city: destination, type, keyword, minPrice, maxPrice, page, limit: "12" }), getPublicPropertyTypes()]);
  const current = result.pagination.page;
  return <section className="marketplace-section"><div className="container"><div className="marketplace-heading"><div><p className="market-eyebrow">CURATED STAYS · {site.city}</p><h1>Find a stay that feels right</h1><p>Verified properties, clear details and genuine local hospitality.</p></div><span>{result.pagination.total} {result.pagination.total === 1 ? "property" : "properties"}</span></div><form className="market-search" action="/hotels"><label><Search/><input name="keyword" defaultValue={keyword} placeholder="Search property, city or location" /></label><select name="type" defaultValue={type || "all"}><option value="all">All property types</option>{types.map((item) => <option key={item._id} value={item.name}>{item.name}</option>)}</select><input name="destination" defaultValue={destination} placeholder="City"/><button type="submit"><SlidersHorizontal/> Search stays</button></form><div className="market-layout"><aside className="market-filters"><p>FILTER YOUR STAY</p><h2>Refine results</h2><form action="/hotels"><input type="hidden" name="keyword" value={keyword || ""}/><input type="hidden" name="destination" value={destination || ""}/><label>Property type<select name="type" defaultValue={type || "all"}><option value="all">All types</option>{types.map((item) => <option key={item._id} value={item.name}>{item.name}</option>)}</select></label><div className="market-price-row"><label>Minimum<input type="number" min="0" name="minPrice" defaultValue={minPrice} placeholder="₹0"/></label><label>Maximum<input type="number" min="0" name="maxPrice" defaultValue={maxPrice} placeholder="Any"/></label></div><button>Apply filters</button></form></aside><div>{result.data.length ? <div className="market-grid">{result.data.map((property) => <PublicPropertyCard key={property._id} property={property}/>)}</div> : <div className="market-empty"><Search/><h2>No properties found</h2><p>Try a different city, property type or a broader price range.</p><Link href="/hotels">Clear all filters</Link></div>}{result.pagination.totalPages > 1 && <nav className="market-pagination" aria-label="Results pages">{Array.from({ length: result.pagination.totalPages }, (_, index) => index + 1).map((value) => <Link className={value === current ? "active" : ""} key={value} href={`/hotels?page=${value}${type ? `&type=${encodeURIComponent(type)}` : ""}${destination ? `&destination=${encodeURIComponent(destination)}` : ""}`}>{value}</Link>)}</nav>}</div></div></div></section>;
}
